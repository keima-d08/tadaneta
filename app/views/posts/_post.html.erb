<div class="post">
<div class = "post.date">
<%= link_to post.user.nickname, user_path(post.user.id), class: :card__user %>
 が<%= post.created_at.strftime('%Y年/%m月/%d日') %>に投稿
</div>
  <div class="title">
    <h3><%= link_to post.title, post_path(post.id),class:"title"%></h3>
    </div>
    ・<%= post.product %>
    ・<%= post.genre.name %>
    ・<%= post.type.name %>
    ・ネタバレ度<%= post.spoil.name %>
</div>