<div class= "main">
 <div class= "follow_wrapper">
  <% @users.each do |user| %>
  <div class= follow_list>
     <div>
      <% if user.image.attached? %>
       <%= image_tag user.image, class: :follow_circle%>
      <% else %>
      eeee
        <% end %>
     </div>
     <div class= follow_date>
     <%= link_to user.nickname,user_path(user.id), class: :prototype__user %>
     </div>
    <% end %>
  </div>
 </div>
</div>